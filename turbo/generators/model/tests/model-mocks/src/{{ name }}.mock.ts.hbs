import type { {{ name }} } from 'app-models/models/{{ name }}Model';
import { generateIdWithPrefix } from "app-models/utils/generateId";
import { faker } from '@faker-js/faker';
import { fn } from '@vitest/spy';
import type { FlattenMaps } from 'mongoose';

export function generate{{ name }}(): FlattenMaps<{{ name }}> {
    return {
        _id: generateIdWithPrefix("{{ prefix }}")(),
        createdAt: faker.date.past(),
        updatedAt: faker.date.past(),
        __v: faker.number.int(),
    }
}

export const Default{{ name }} = generate{{ name }}();
