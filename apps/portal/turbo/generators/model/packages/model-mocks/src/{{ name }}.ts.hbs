import type { {{ name }} } from '@comradesharf/models/models/{{ name }}';
import { generateIdWithPrefix } from '@comradesharf/models/models/Base';
import { faker } from '@faker-js/faker';
import { fn } from '@vitest/spy';
import type { FlattenMaps } from 'mongoose';

export function generate{{ name }}(): FlattenMaps<{{ name }}> {
    return {
        _id: generateIdWithPrefix("{{ prefix }}")(),
        created_at: faker.date.past(),
        updated_at: faker.date.past(),
    }
}

export const Default{{ name }} = generate{{ name }}();
