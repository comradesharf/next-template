'use server';

import "server-only";
import { actionClient } from '#libs/safe-actions.ts';
{{#if schema}}
import { {{ pascalCase name }}Schema } from '@comradesharf/schemas/{{ pascalCase name }}Schema';
{{/if}}

export const {{name}} = actionClient
    .metadata({
        actionName: '{{name}}',
    })
{{#if schema}}
    .schema({{ pascalCase name }}Schema)
{{/if}}
    .action(async () => {});
